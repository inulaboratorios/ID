/* ============================================================
   Inu ID — Configuración
   ------------------------------------------------------------
   DEMO = true  -> la app funciona con datos de mentira, sin
                   llamar al servidor. Úsalo para ver el front
                   localmente o en la vista previa.
   DEMO = false -> la app habla con la API real de Jose.
                   Ponlo en false cuando lo subas a Vercel.
   ============================================================ */
window.INUID_CONFIG = {
  DEMO: true,
  API_BASE: "https://api.inulaboratorios.com/api"
};
